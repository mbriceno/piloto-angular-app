'use strict';

/**
 * @ngdoc function
 * @name pilotoApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the pilotoApp
 */

angular.module('pilotoApp')
  .controller('MainCtrl', ['$rootScope', '$scope', 'toaster', '$state', 'SessionService', '$anchorScroll', '$location', '$filter',
  function ($rootScope, $scope, toaster, $state, SessionService, $anchorScroll, $location, $filter) {

  }]);
